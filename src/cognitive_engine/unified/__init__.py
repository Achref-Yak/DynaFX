"""Unified reasoning framework — Master Equation and orchestration.

Usage:
    from cognitive_engine.unified import UnifiedReasoner, Coefficients
    reasoner = UnifiedReasoner(Coefficients())
    result = reasoner.reason(graph)
"""
